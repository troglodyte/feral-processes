# Todos
[ ] some ambiguity|routines for examining entities
[ ] examine entities in stacks? (boss?)
[ ] examine passes straight through nests, surface links and zone portals —
    all three draw a glyph but carry neither Creature nor Structure, so
    aiming at one reports whatever lies beyond it. Cheapest route is
    Mode::CellDescribe, already a generic "here is a paragraph, any key
    closes" popup: the engine returns a sentence, no new mode and no new
    gui file, at the cost of renaming draw_cell_describe (it would stop
    being Stack-only). A full sheet per kind is the expensive route —
    an InspectTarget variant, a report type, a Mode, a renderer, per kind.
[ ] environment effects
[.] unorthodox solutions
[ ] high stakes reward
[ ] easter eggs (hidden commands & effects)
[ ] remove 'move abilities' and use routines
[ ] save game binaries so people don't have to compile the game
[ ] more machine learning
[ ] seed ephemeral markets in stacks with routines
    - randomly generated ones
    - single 150cr 
    - party 300cr
    - everyone 1000cr
[ ] i want the description of what i'm seeing as i'm walking in the stack, not with x | examine
# Bugs

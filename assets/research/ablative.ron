(
    id: "ablative",
    name: "Ablative Lattice",
    description: "Plate that sheds a layer per hit so the process behind it does not. Compiled at an Armory.",
    cost: 110,
    materials: [("hardened_shell", 8), ("bytecode_block", 24)],
    min_zone: 3,
    requires: ["firewall"],
    unlocks_recipes: [(
        result: "ablative_plating",
        cost: [("portal_fragment", 12), ("cache_grain", 3)],
        requires_structure: Some("armory"),
    )],
)

(
    id: "cortex",
    name: "Cortex Hacking",
    description: "A module that speaks to a hostile program in its own dialect, compiled at a Fabricator. Also the reader that goes through a downed process for the routines it was running and keeps one.",
    cost: 125,
    materials: [("trace_sniffer", 6), ("logic_wafer", 12)],
    min_zone: 3,
    requires: ["neural_amp"],
    unlocks_tools: ["routine_reader"],
    unlocks_recipes: [(
        result: "cortex_hack",
        cost: [("portal_fragment", 12), ("cache_grain", 3)],
        requires_structure: Some("fabricator"),
    )],
)

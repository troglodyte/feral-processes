(
    id: "deep_analysis",
    name: "Deep Analysis",
    description: "Field routines that bend the odds of a whole run rather than a single fight, compiled into cargo. Also the two extraction tools: one draws the compiled core out of a downed process, the other pulls the worn gear off it.",
    cost: 130,
    materials: [("routine_disk", 16), ("logic_wafer", 16)],
    min_zone: 3,
    requires: ["field_ops"],
    unlocks_abilities: ["deep_scan", "trace_analysis", "stealth_protocol", "salvage_routine"],
    unlocks_tools: ["core_tap", "harness_puller"],
)

(
    id: "program_refactoring",
    name: "Program Refactoring",
    description: "Rewrite a tamed program instead of replacing it — or pull one apart for parts instead of scrap.",
    cost: 75,
    materials: [("bytecode_block", 24), ("charge_coil", 12)],
    min_zone: 2,
    requires: ["automation"],
    unlocks_structures: ["annealing_node", "refactor_bench"],
    unlocks_tools: ["component_stripper"],
)

(
    id: "assembly_bay",
    name: "Assembly Bay",
    description: "Assign a program here and it builds Patch Routines out of a Bytecode Block and a Charge Coil. It needs both feeders touching it, so it wants a corner.",
    glyph: 'Y',
    color: Magenta,
    build_cost: [("core_fragment", 20), ("bytecode_block", 4)],
    work: None,
    capacity: 10,
    assembles: Some((item: "patch_routine", ticks_per_unit: 20)),
)

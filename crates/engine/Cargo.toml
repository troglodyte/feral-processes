[package]
name = "feral-processes-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy_ecs = "0.19.0"
bincode = { version = "2", features = ["serde"] }
noise = "0.9.0"
rand = "0.10.2"
ron = "0.12.2"
serde = { version = "1.0.228", features = ["derive"] }
